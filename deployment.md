# 将 Jackify 部署到 Cloudflare Pages - 详细指南

本指南将指导您完成将网站部署到 Cloudflare Pages 的全过程。

---

## 🛑 重要提示：文件与目录结构
在执行任何命令之前，请确保您的项目文件结构如下（在 `C:\Users\suqiu\Desktop\jackify` 文件夹内）：

```text
jackify/
├── node_modules/       (由 npm install 生成)
├── components/         (包含 Navbar.tsx, Hero.tsx 等)
├── App.tsx
├── index.html          (必须包含 <script type="module" src="/index.tsx"></script>)
├── index.tsx           (这是项目的入口文件)
├── metadata.json
├── package.json
├── tsconfig.json
├── types.ts
└── vite.config.ts      (如果有的话)
```

---

## 第一步：本地测试与构建

**操作位置**：打开您的终端（Terminal）或命令行（Command Prompt）。
**当前路径**：确保路径显示为 `C:\Users\suqiu\Desktop\jackify>`。

1.  **安装依赖**（如果尚未安装）：
    此命令会下载 `package.json` 中列出的所有库（如 React, Tailwind 等）。
    ```bash
    npm install
    ```

2.  **本地构建测试**：
    此命令会将您的 TypeScript 代码编译为浏览器可读的 HTML/CSS/JS 文件。
    ```bash
    npm run build
    ```
    *   **成功标志**：如果显示 `dist/index.html ...` 和 `✓ built in ...`，说明构建成功。
    *   **生成文件**：这会在您的文件夹中创建一个名为 `dist` 的新文件夹。

---

## 第二步：上传代码到 GitHub

**操作位置**：继续在同一个终端窗口操作。

1.  **初始化 Git**：
    ```bash
    git init
    ```

2.  **添加文件**：
    ```bash
    git add .
    ```

3.  **提交更改**：
    ```bash
    git commit -m "Initial commit"
    ```

4.  **关联远程仓库**：
    *   去 [GitHub](https://github.com/) 创建一个新的仓库（Repository），名字叫 `jackify`。
    *   复制 GitHub 给您的类似下面的命令并在终端运行：
    ```bash
    git remote add origin https://github.com/您的用户名/jackify.git
    git branch -M main
    git push -u origin main
    ```

---

## 第三步：在 Cloudflare Pages 上部署

**操作位置**：您的浏览器（Chrome/Edge 等）。

1.  登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。
2.  在左侧菜单点击 **Workers & Pages**。
3.  点击蓝色的 **Create Application** 按钮。
4.  点击 **Pages** 标签页，然后点击 **Connect to Git**。
5.  选择您的 GitHub 账号，并选择刚才上传的 **`jackify`** 仓库。
6.  点击 **Begin setup**（开始设置）。

### 关键配置设置（Build Settings）

在配置页面，请确保填写以下信息：

*   **Project Name**: jackify
*   **Production Branch**: main
*   **Framework Preset** (框架预设): 选择 **Vite** (这非常重要！)
*   **Build command** (构建命令): `npm run build`
*   **Build output directory** (构建输出目录): `dist`

点击 **Save and Deploy**（保存并部署）。

---

## 第四步：故障排除

如果在 Cloudflare 上部署失败：

1.  **检查构建命令**：确保是 `npm run build`。
2.  **检查输出目录**：确保是 `dist`。
3.  **根目录设置**：如果您的代码是直接放在仓库根目录（没有套一层文件夹），**Root directory** 这一栏请**留空**（不要填 `/` 或 `src`）。